import {
  MINIO_ROOT_PASSWORD,
  MINIO_ROOT_USER,
} from 'src/constants/environment';

export const config = {
  MINIO_ENDPOINT: 'localhost',
  MINIO_PORT: 9000,
  MINIO_ACCESSKEY: 'ROOTNAME',
  MINIO_SECRETKEY: 'CHANGEME123',
  MINIO_BUCKET: 'dasdasdas',
};
